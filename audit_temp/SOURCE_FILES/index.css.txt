@import url('https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;800;900&display=swap');
@import "tailwindcss";

@theme {
  --font-sans: "Cairo", ui-sans-serif, system-ui, sans-serif;
}

html, body {
  font-family: 'Cairo', sans-serif !important;
  direction: rtl !important;
  text-align: right !important;
  background-color: #f8fafc;
}

/* Force Cairo font globally on all widgets and inputs */
*, *::before, *::after {
  font-family: 'Cairo', sans-serif !important;
}

/* Material UI global overrides to strictly enforce RTL direction on portals and widgets */
[class*="Mui"],
.MuiPaper-root,
.MuiDialog-root,
.MuiPopover-root,
.MuiMenu-root,
.MuiAutocomplete-popper,
.MuiTooltip-popper,
.MuiDrawer-root,
.MuiModal-root {
  direction: rtl !important;
}

/* Enforce center alignment for all table cells in MUI tables */
.MuiTableCell-root,
.MuiTableCell-head,
.MuiTableCell-body {
  text-align: center !important;
}

/* Print CSS Media query to export exceptionally beautiful PDFs */
@media print {
  /* Hide all generic UI components, sidebar, header, buttons and layout margins */
  body {
    background-color: #ffffff !important;
    background: #ffffff !important;
    color: #000000 !important;
    margin: 0 !important;
    padding: 0 !important;
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
  }
  
  .no-print-area, 
  aside, 
  header, 
  nav, 
  button, 
  .MuiButton-root,
  .MuiTablePagination-root {
    display: none !important;
    visibility: hidden !important;
    height: 0 !important;
    overflow: hidden !important;
    opacity: 0 !important;
  }

  /* Make the printable container expand to occupy the full page width cleanly with zero margins */
  #printable-reportZone {
    position: absolute !important;
    left: 0 !important;
    top: 0 !important;
    width: 100% !important;
    border: none !important;
    box-shadow: none !important;
    padding: 0 !important;
    margin: 0 !important;
    visibility: visible !important;
  }

  /* Force background colors on tables and headers during printing */
  .MuiTableHead-root, thead {
    background-color: #f1f5f9 !important;
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
  }

  th, td, .meta-label, .meta-value {
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
    background-clip: padding-box !important;
    box-sizing: border-box !important;
  }

  tr {
    page-break-inside: avoid !important;
  }
  
  thead {
    display: table-header-group !important;
  }
  
  tfoot {
    display: table-footer-group !important;
  }
}

/* Hide up/down arrows (spinners) on number inputs */
input::-webkit-outer-spin-button,
input::-webkit-inner-spin-button {
  -webkit-appearance: none !important;
  margin: 0 !important;
}
input[type=number] {
  -moz-appearance: textfield !important;
}

/* Force English/Latin font and digits on numeric and date input fields */
input[type=number],
input[type=date],
input[type=datetime-local],
.english-digits-input {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif !important;
  direction: ltr !important;
  text-align: right !important;
}


/* Improve dropdown menus font - added for Emoji request */
.MuiMenuItem-root {
  font-family: 'Cairo', sans-serif !important;
  font-size: 0.95rem !important;
  font-weight: 500 !important;
  padding-top: 8px !important;
  padding-bottom: 8px !important;
}

.MuiSelect-select {
  font-family: 'Cairo', sans-serif !important;
  font-weight: 600 !important;
  font-size: 0.95rem !important;
  display: flex !important;
  align-items: center !important;
  gap: 8px !important;
}

/* User Card Styles for consistent grid layout and wrapping */
.user-card {
  width: 100% !important;
  min-height: 220px !important;
  display: flex !important;
  flex-direction: column !important;
  justify-content: space-between !important;
}

.user-card .MuiCardContent-root {
  flex-grow: 1 !important;
  display: flex !important;
  flex-direction: column !important;
}
